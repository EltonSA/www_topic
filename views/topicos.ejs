<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Tópicos Cadastrados</title>
    <!-- Adicionando o Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background-color: #f8f9fa;
            font-family: 'Arial', sans-serif;
        }
        .container {
            max-width: 900px;
            margin: 40px auto;
            background-color: #ffffff;
            padding: 20px;
            box-shadow: 0 0 15px rgba(0, 0, 0, 0.1);
            border-radius: 10px;
        }
        .content {
            margin-top: 20px;
        }
        .list-group-item {
            border: 1px solid rgba(0, 0, 0, 0.125);
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 15px;
            transition: background-color 0.2s;
        }
        .list-group-item:hover {
            background-color: #f0f0f0;
        }
        .btn-primary {
            margin-bottom: 20px;
        }
        .search-form {
            display: flex;
            justify-content: space-between;
            align-items: center;
        }
        .search-form .input-group {
            width: 100%;
            max-width: 500px;
        }
        .topic-link {
            text-decoration: none;
            color: inherit;
            font-weight: bold;
            flex-grow: 1;
        }
        .topic-link:hover {
            color: #007bff;
        }
        .topic-date {
            color: #6c757d;
            font-size: 0.9em;
            margin-left: 10px;
        }
        .icon {
            color: #6c757d;
            margin-right: 10px;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="content">
            <!-- Título, botão e barra de pesquisa na mesma linha -->
            <div class="d-flex justify-content-between align-items-center mb-4">
                <h1 class="mb-0">Tópicos Cadastrados</h1>
                <!-- Botão "Cadastrar Novo Tópico" -->
                <a href="/" class="btn btn-primary">Cadastrar Novo Tópico</a>
            </div>
            <!-- Formulário de pesquisa -->
            <form action="/topicos" method="GET" class="search-form mb-4">
                <div class="input-group">
                    <input type="text" class="form-control" placeholder="Pesquisar tópicos..." name="q">
                    <button class="btn btn-outline-primary" type="submit">Pesquisar</button>
                </div>
            </form>
            <ul class="list-group">
                <% topics.forEach(topic => { %>
                    <!-- Transformar o título do tópico em um link -->
                    <li class="list-group-item">
                        <i class="icon bi bi-file-earmark-text"></i>
                        <a href="/topic/<%= topic._id %>" class="topic-link"><%= topic.title %></a>
                    </li>
                <% }); %>
            </ul>
        </div>
    </div>
    <!-- Adicionando o Bootstrap JS (opcional, caso utilize componentes interativos do Bootstrap) -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <!-- Adicionando o Bootstrap Icons -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/bootstrap-icons/1.5.0/font/bootstrap-icons.min.css">
</body>
</html>
