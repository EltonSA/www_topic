<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Tópicos Cadastrados</title>
    <!-- Adicionando o Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        /* Estilo personalizado para o conteúdo */
        body {
            padding-top: 20px;
            background-color: #f8f9fa;
        }
        .container {
            max-width: 800px;
            margin: 0 auto;
        }
        .content {
            margin-top: 20px;
        }
        .list-group-item {
            background-color: #fff;
            border: 1px solid rgba(0, 0, 0, 0.125);
            border-radius: 0;
        }
        .list-group-item:hover {
            background-color: #f0f0f0;
        }
        .btn-primary {
            margin-bottom: 20px;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="content">
            <!-- Título, botão e barra de pesquisa na mesma linha -->
            <div class="d-flex justify-content-between align-items-center mb-3">
                <h1>Tópicos Cadastrados</h1>
                <!-- Botão "Cadastrar Novo Tópico" -->
                <a href="/" class="btn btn-primary">Cadastrar Novo Tópico</a>
            </div>
            <!-- Formulário de pesquisa -->
            <form action="/topicos" method="GET" class="mb-3">
                <div class="input-group">
                    <input type="text" class="form-control" placeholder="Pesquisar tópicos..." name="q">
                    <button class="btn btn-outline-primary" type="submit">Pesquisar</button>
                </div>
            </form>
            <ul class="list-group">
                <% topics.forEach(topic => { %>
                    <!-- Transformar o título do tópico em um link -->
                    <a href="/topic/<%= topic._id %>" class="list-group-item list-group-item-action">
                        <b><%= topic.title %></b>
                    </a>
                <% }); %>
            </ul>
        </div>
    </div>
    <!-- Adicionando o Bootstrap JS (opcional, caso utilize componentes interativos do Bootstrap) -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
